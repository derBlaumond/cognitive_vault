좋아, 이제 **{03_basic_programming.md}**, 즉 _Basic Programming_ 강의 내용을  
순서대로 생략 없이 한국어로 정리해줄게.  
이번 노트는 각 소단원별로 원문의 논리 흐름을 유지하면서, 실제 예제 코드나 개념을 한국어로 풀어 설명했어.

---

# 📘 Basic Programming — 정리 노트

---

## 1️⃣ 프로그래밍이란 무엇인가? (What is Programming?)

- **프로그래밍(Programming)**:  
    컴퓨터가 이해하고 수행할 수 있도록 **명령을 작성하는 과정**.
    
- **목적:** 문제 해결(Problem Solving).  
    → 복잡한 문제를 **논리적 단계(algorithm)** 로 나누고 이를 코드로 구현한다.
    

### 예시:

- 계산기 앱을 만들 때:
    
    1. 사용자 입력 받기
        
    2. 사칙연산 수행
        
    3. 결과 출력  
        → 이 전체 과정이 프로그래밍.
        

---

## 2️⃣ 프로그램과 프로그래밍의 차이

|구분|프로그램 (Program)|프로그래밍 (Programming)|
|---|---|---|
|정의|특정 목적을 수행하는 명령어 집합|프로그램을 설계·작성·수정하는 과정|
|결과물|실행 가능한 소프트웨어|문제 해결을 위한 논리적 절차|
|예시|계산기, 게임, 웹사이트|코드 작성, 디버깅, 테스트|

---

## 3️⃣ 알고리즘 (Algorithm)

- **정의:**  
    문제를 해결하기 위한 단계별 명령어의 집합.  
    → 프로그래밍의 “논리적 청사진”.
    
- **특징 (4가지):**
    
    1. 유한성 (Finite) — 반드시 종료되어야 함
        
    2. 명확성 (Definiteness) — 각 단계가 모호하지 않아야 함
        
    3. 입력 (Input) — 외부 데이터 입력 가능
        
    4. 출력 (Output) — 결과를 반드시 생성
        

### 예시:

“세 수 중 가장 큰 수 찾기” 알고리즘

1. 세 수 A, B, C를 입력받는다.
    
2. A > B인지 비교.
    
3. 참이면 A와 C 비교 → 더 큰 값을 출력.
    
4. 거짓이면 B와 C 비교 → 더 큰 값을 출력.
    

---

## 4️⃣ 알고리즘 표현 방식

1. **의사코드(Pseudocode)**
    
    - 자연어와 프로그래밍 구문을 섞은 형태.
        
    - 언어 독립적이어서 초보자에게 이해하기 쉬움.
        
    
    **예시:**
    
    ```
    Start
    Input A, B
    If A > B then
        Print "A is greater"
    Else
        Print "B is greater"
    End
    ```
    
2. **순서도(Flowchart)**
    
    - 알고리즘의 논리 흐름을 시각적으로 표현.
        
    - 도형의 의미:
        
        - ● Start/End: 타원
            
        - ▭ Process: 사각형
            
        - ◇ Decision: 마름모
            
        - ⧍ Input/Output: 평행사변형
            
        - →: 흐름선(Flow line)
            
    
    **예시:**  
    입력 → 비교 → 결과 출력 → 종료
    

---

## 5️⃣ 프로그래밍 패러다임 (Programming Paradigms)

- 프로그램을 구조화하는 사고 방식.
    
- 대표 3가지 패러다임:
    

|패러다임|특징|대표 언어|
|---|---|---|
|**절차적 (Procedural)**|명령의 순서 중심, 단계별 처리|C, Pascal|
|**객체지향 (Object-Oriented)**|객체 간 상호작용 중심|Java, C++, Python|
|**함수형 (Functional)**|상태 변화 없이 함수 조합으로 구성|Haskell, Scala, JS(함수형 기법)|

---

## 6️⃣ 절차적 프로그래밍 (Procedural Programming)

- **핵심 개념:**  
    프로그램을 **“함수(Procedure)” 단위로 나누어 실행**.
    
- **장점:** 단순하고 명확한 흐름, 디버깅 용이.
    
- **단점:** 프로그램이 커지면 관리가 어려움.
    

### 예시: Python

```python
def add(a, b):
    return a + b

result = add(5, 3)
print(result)
```

---

## 7️⃣ 객체지향 프로그래밍 (Object-Oriented Programming, OOP)

- **핵심 개념:**  
    실제 세상을 모델링하여, 데이터를 **객체(Object)** 단위로 표현.
    

### 주요 구성요소:

|개념|설명|예시|
|---|---|---|
|**Class**|객체의 청사진(설계도)|`class Car:`|
|**Object**|클래스의 인스턴스(실체)|`my_car = Car()`|
|**Method**|객체가 수행하는 동작|`start(), stop()`|
|**Property**|객체의 속성|`color, model`|

### 예시: Python

```python
class Car:
    def __init__(self, color):
        self.color = color

    def start(self):
        print("Engine started")

my_car = Car("red")
my_car.start()
```

### OOP의 4대 특징:

1. **Encapsulation (캡슐화)** – 데이터 은닉
    
2. **Abstraction (추상화)** – 불필요한 세부사항 감춤
    
3. **Inheritance (상속)** – 코드 재사용
    
4. **Polymorphism (다형성)** – 같은 이름의 메서드가 다양한 행동
    

---

## 8️⃣ 함수형 프로그래밍 (Functional Programming)

- **정의:** 상태(변수) 변경 없이 **함수의 조합으로 연산 수행.**
    
- **특징:** 불변성(immutability), 순수 함수(pure function).
    
- **장점:** 버그 발생 가능성 낮음, 병렬처리 유리.
    
- **예시:** JavaScript
    
    ```js
    const add = (x, y) => x + y;
    const result = [1, 2, 3].map(x => add(x, 5));
    ```
    

---

## 9️⃣ 변수와 데이터 타입 (Variables and Data Types)

- **변수:** 데이터를 저장하기 위한 이름 있는 공간.  
    → `x = 10`
    
- **데이터 타입:** 변수에 저장되는 값의 형태.
    

|타입|설명|예시|
|---|---|---|
|**int**|정수|`x = 5`|
|**float**|실수|`pi = 3.14`|
|**str**|문자열|`"Hello"`|
|**bool**|논리값|`True`, `False`|
|**list**|순서 있는 자료집합|`[1, 2, 3]`|
|**dict**|키-값 쌍의 집합|`{"name":"Tom"}`|

---

## 🔟 연산자 (Operators)

### 산술 연산자

|연산자|의미|예시|
|---|---|---|
|`+`|덧셈|`a + b`|
|`-`|뺄셈|`a - b`|
|`*`|곱셈|`a * b`|
|`/`|나눗셈|`a / b`|
|`%`|나머지|`a % b`|

### 비교 연산자

|연산자|의미|예시|
|---|---|---|
|`==`|같다|`a == b`|
|`!=`|다르다|`a != b`|
|`>` / `<`|크다 / 작다|`a > b`|
|`>=` / `<=`|크거나 같다 / 작거나 같다|`a >= b`|

### 논리 연산자

|연산자|설명|
|---|---|
|`and`|두 조건 모두 참일 때 True|
|`or`|하나라도 참이면 True|
|`not`|반대 값 반환|

---

## 11️⃣ 제어문 (Control Statements)

### 조건문

```python
if age >= 18:
    print("Adult")
else:
    print("Minor")
```

### 반복문

- `for` 루프:
    
    ```python
    for i in range(5):
        print(i)
    ```
    
- `while` 루프:
    
    ```python
    count = 0
    while count < 3:
        print(count)
        count += 1
    ```
    

---

## 12️⃣ 배열과 리스트 (Arrays & Lists)

- **리스트(list):** 여러 데이터를 순서 있게 저장.
    
    ```python
    fruits = ["apple", "banana", "cherry"]
    print(fruits[1])  # banana
    ```
    
- **배열(array):** 동일한 자료형만 저장 (Python은 list로 대체 가능).
    

---

## 13️⃣ 함수 (Functions)

- **정의:** 재사용 가능한 코드 블록.
    
- **형식:**
    
    ```python
    def greet(name):
        print("Hello", name)
    greet("Injun")
    ```
    
- **반환값:** `return` 키워드 사용.
    
    ```python
    def add(a, b):
        return a + b
    ```
    
- **이점:** 코드 재사용, 유지보수성 향상, 가독성 증가.
    

---

## 14️⃣ 디버깅과 오류 처리 (Debugging & Error Handling)

- **디버깅(Debugging):** 코드의 오류를 찾아 수정하는 과정.
    
- **오류 유형:**
    
    - **Syntax Error (문법 오류)** → 컴파일 시 감지
        
    - **Runtime Error (실행 오류)** → 실행 중 발생
        
    - **Logic Error (논리 오류)** → 잘못된 결과 출력
        

### Python 예시:

```python
try:
    print(10 / 0)
except ZeroDivisionError:
    print("Cannot divide by zero")
finally:
    print("Done")
```

---

## 15️⃣ 내부자 관점 (Insider’s Viewpoint)

- **전문가들의 조언:**
    
    - “프로그래밍은 단순한 코딩이 아니라 사고의 훈련이다.”
        
    - “처음에는 작게, 명확하게 시작하라.”
        
    - “디버깅은 개발의 일부이며, 실패는 학습 과정이다.”
        
    - “모든 언어의 핵심은 논리 구조와 문제 해결 능력.”
        

---

## 🧠 전체 요약

|주제|핵심 내용|
|---|---|
|**프로그래밍 정의**|명령어로 문제를 해결하는 과정|
|**알고리즘**|문제 해결의 논리적 절차|
|**프로그래밍 패러다임**|절차적, 객체지향, 함수형|
|**데이터 타입과 변수**|값 저장 및 연산의 기본 단위|
|**연산자 / 제어문 / 함수**|로직 구성의 핵심 문법|
|**디버깅**|오류 탐색 및 수정 과정|
|**전문가 조언**|반복 학습과 논리적 사고의 중요성|

---

이 노트는 `{03_basic_programming.md}`의 전체 내용을  
**원문 순서 그대로**, **생략 없이**, **이해하기 쉽게 재구성한 정리본**이야.  
원하면 다음으로 `{04_sw_architecture_design_pattern.md}` 파일도 같은 방식으로 이어서 정리해줄까?