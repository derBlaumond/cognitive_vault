# DCP AI Expert Service - 상세 발표 자료

> 코드베이스 분석 기반 상세 기술 문서

---

## 1. 프로젝트 개요

### 1.1 프로젝트 소개

**DCP AI Expert Service**는 SAP Digital Commerce Platform (DCP) 관련 문서를 기반으로 질문에 답변하는 RAG (Retrieval Augmented Generation) 기반 AI 서비스입니다.

### 1.2 핵심 목적

1. **문서 기반 질의응답**: DCP 관련 비즈니스 시나리오 및 기술 문서에 대한 지능형 질의응답 제공
2. **A2A 프로토콜 준수**: Agent-to-Agent 프로토콜을 준수하여 다른 A2A 에이전트와 통합 가능
3. **SAP HANA 통합**: SAP HANA Cloud Vector Engine을 활용한 효율적인 문서 검색
4. **SAP AI Hub 통합**: SAP Generative AI Hub를 통한 LLM 기반 응답 생성

### 1.3 애플리케이션 진입점

애플리케이션은 `__main__.py` 파일을 통해 시작됩니다. 메인 모듈 런처는 다음과 같은 역할을 수행합니다:

**실행 흐름**:
1. **AIExpertService 초기화**: 애플리케이션의 메인 클래스를 초기화하여 에이전트를 로드하고 라우트를 등록합니다.
2. **Flask 앱 및 SocketIO 서버 시작**: 웹 인터페이스를 제공하기 위해 Flask 애플리케이션과 SocketIO 서버를 시작합니다.
3. **웹 인터페이스 제공**: 기본적으로 포트 5000에서 웹 인터페이스를 제공합니다.

애플리케이션은 Gunicorn을 통한 WSGI 서버 실행도 지원하도록 설계되어 있으나, 현재는 SocketIO를 통한 직접 실행 방식을 사용하고 있습니다.

### 1.4 메인 애플리케이션 클래스

`AIExpertService` 클래스는 전체 애플리케이션의 핵심입니다. 이 클래스는 다음과 같은 초기화 단계를 거칩니다:

**초기화 단계**:
1. **설정 파일 로드**: `app.yaml` 파일에서 애플리케이션 설정을 읽어옵니다.
2. **환경 변수 로드**: `.env` 파일에서 환경 변수를 로드합니다. 이는 데이터베이스 연결 정보, API 키 등 민감한 정보를 포함합니다.
3. **로거 초기화**: 애플리케이션 전체에서 사용할 로거를 설정합니다. 로그 레벨은 설정 파일에서 지정됩니다.
4. **에이전트 라우터 초기화**: `AgentRouter`를 생성하여 모든 활성 에이전트를 로드합니다. 이 과정에서 `agents.yaml` 파일의 설정을 읽어 각 에이전트를 초기화합니다.
5. **Flask 라우트 및 SocketIO 이벤트 등록**: 웹 인터페이스의 라우트와 실시간 통신을 위한 이벤트 핸들러를 등록합니다.

초기화가 완료되면, 애플리케이션은 사용자 요청을 처리할 준비가 됩니다.

---

## 2. 핵심 기능

### 2.1 RAG (Retrieval Augmented Generation)

#### 2.1.1 RAG 에이전트 구조

RAG 에이전트는 `RAGAgentLogic` 클래스로 구현되어 있습니다. 이 클래스는 문서 기반 질의응답을 위한 모든 기능을 담당합니다.

**주요 구성 요소**:
- **설정 관리**: `agents.yaml`에서 해당 에이전트의 설정을 읽어옵니다.
- **HANA 연결 컨텍스트**: SAP HANA Cloud Vector Engine과의 연결을 관리합니다.
- **임베딩 모델**: 문서와 질문을 벡터로 변환하는 모델입니다.
- **벡터 스토어**: 각 청킹 전략별로 별도의 벡터 스토어를 관리합니다.
- **LLM 모델**: 답변을 생성하는 언어 모델입니다.
- **RAG 체인**: Retriever와 Document Chain을 결합한 RAG 파이프라인입니다.

#### 2.1.2 RAG 초기화 프로세스

RAG 에이전트는 초기화 시 다음 순서로 컴포넌트를 준비합니다:

**초기화 순서**:
1. **HANA 연결**: SAP HANA Cloud Vector Engine에 연결합니다. 연결 정보는 환경 변수에서 가져옵니다.
2. **임베딩 모델 초기화**: OpenAI Embeddings 모델을 초기화합니다. 이 모델은 문서와 질문을 벡터로 변환하는 데 사용됩니다.
3. **벡터 스토어 설정**: 각 청킹 전략별로 벡터 스토어를 생성합니다. 각 전략은 고유한 테이블 이름을 가집니다.
4. **LLM 모델 초기화**: SAP AI Hub의 LLM 모델을 초기화합니다. 현재는 GPT-4O Mini를 사용합니다.
5. **RAG 체인 구성**: Retriever와 Document Chain을 결합하여 RAG 체인을 생성합니다. 이 체인은 질문을 받아 관련 문서를 검색하고 답변을 생성합니다.

초기화가 완료되면, 에이전트는 사용자 질문을 처리할 준비가 됩니다.

#### 2.1.3 RAG 체인 구성

RAG 체인은 LangChain의 Retrieval Chain을 기반으로 구성됩니다. 체인은 다음과 같은 구성 요소로 이루어져 있습니다:

**RAG 체인 구성 요소**:
- **Retriever**: 벡터 스토어에서 질문과 유사한 문서를 검색합니다. 검색 전략은 설정에서 지정할 수 있으며, MMR (Maximal Marginal Relevance), Similarity, Score Threshold 등을 지원합니다.
- **Prompt Template**: 에이전트별로 정의된 프롬프트 템플릿입니다. 이 템플릿은 검색된 문서를 컨텍스트로 사용하여 LLM에 전달합니다.
- **Document Chain**: 검색된 문서를 LLM에 전달하여 답변을 생성하는 체인입니다.
- **Retrieval Chain**: Retriever와 Document Chain을 통합한 최종 체인입니다.

체인은 청킹 전략과 LLM 모델의 조합별로 생성되며, 필요에 따라 적절한 체인을 선택하여 사용합니다.

### 2.2 다중 에이전트 지원

#### 2.2.1 에이전트 라우터

`AgentRouter` 클래스는 모든 에이전트를 관리하고 요청을 적절한 에이전트로 라우팅하는 중앙 관리자 역할을 합니다.

**주요 기능**:
- **에이전트 초기화**: 설정 파일에서 활성 에이전트를 읽어 동적으로 로드합니다.
- **요청 라우팅**: 사용자 요청을 선택된 에이전트로 전달합니다.
- **응답 래핑**: 에이전트의 응답을 일관된 형식으로 래핑하여 반환합니다.

#### 2.2.2 에이전트 초기화

에이전트 초기화는 다음과 같은 프로세스를 따릅니다:

**초기화 프로세스**:
1. **설정 파일 읽기**: `agents.yaml` 파일에서 모든 에이전트 설정을 읽습니다.
2. **활성 에이전트 필터링**: `active: true`로 설정된 에이전트만 로드합니다. 비활성 에이전트는 건너뜁니다.
3. **동적 임포트**: Python의 `importlib`를 사용하여 에이전트 클래스를 동적으로 임포트합니다. 이를 통해 설정 파일만 수정하여 새 에이전트를 추가할 수 있습니다.
4. **인스턴스 생성**: 각 에이전트 클래스의 인스턴스를 생성하고 딕셔너리에 저장합니다.

이 방식의 장점은 UI 코드를 수정하지 않고도 새 에이전트를 추가할 수 있다는 것입니다.

#### 2.2.3 에이전트 호출

에이전트 호출은 다음과 같은 흐름으로 진행됩니다:

**호출 흐름**:
1. **질문 전처리**: 사용자가 요청한 경우, AICore 에이전트를 사용하여 질문의 오타를 수정합니다.
2. **세션 메시지 추가**: 사용자 질문을 세션의 메시지 히스토리에 추가합니다.
3. **에이전트 호출**: 선택된 에이전트의 `invoke()` 메서드를 호출하여 질문을 처리합니다.
4. **응답 래핑**: 에이전트의 응답을 일관된 형식으로 래핑합니다. 이 과정에서 메타데이터(응답 시간, 타임스탬프, 연결 모드 등)를 추가합니다.

응답은 성공 여부, 답변 내용, 출처 문서, 메타데이터 등을 포함하는 딕셔너리 형식으로 반환됩니다.

### 2.3 연결 모드

현재 애플리케이션은 여러 연결 모드를 지원하도록 설계되어 있습니다:

**구현된 모드**:
- **Direct Mode**: SAP AI Core에 직접 연결하여 빠른 응답을 제공합니다. 현재 기본 모드로 사용됩니다.

**향후 구현 예정**:
- **Agent Proxy Mode**: A2A 서버를 통한 연결로, 더 안정적이지만 상대적으로 느립니다.
- **Hybrid Mode**: Direct Mode를 먼저 시도하고 실패 시 Agent Proxy로 폴백하는 지능형 모드입니다.

### 2.4 웹 인터페이스

#### 2.4.1 Flask 서버 초기화

Flask 애플리케이션은 `modules/web/server.py`에서 초기화됩니다. 서버는 다음과 같은 구성 요소를 포함합니다:

**구성 요소**:
- **Flask 앱 인스턴스**: 템플릿 폴더와 정적 파일 폴더를 설정합니다.
- **세션 관리**: Flask-Session을 사용하여 사용자 세션을 관리합니다. 현재는 파일 시스템 기반 세션을 사용하며, 향후 Redis 기반 세션도 지원할 수 있도록 설계되어 있습니다.
- **SocketIO 인스턴스**: 실시간 통신을 위한 SocketIO 서버입니다. Eventlet 모드를 사용합니다.

#### 2.4.2 주요 라우트

애플리케이션은 다음과 같은 주요 페이지를 제공합니다:

**주요 페이지**:
- **`/` (Workspace)**: 랜딩 페이지 또는 워크스페이스입니다. 로그인하지 않은 사용자에게는 로그인 페이지를 보여주고, 로그인한 사용자에게는 워크스페이스를 보여줍니다.
- **`/rag`**: RAG 질의응답 인터페이스입니다. 사용자가 질문을 입력하고 에이전트를 선택하여 답변을 받을 수 있습니다. 로그인이 필요합니다.
- **`/sidebar`**: 사이드바 컴포넌트입니다.
- **`/settings`**: 설정 페이지입니다. 관리자 권한이 필요하며, 애플리케이션 및 에이전트 설정을 확인할 수 있습니다.

#### 2.4.3 RAG 페이지 처리

RAG 페이지는 GET과 POST 요청을 모두 처리합니다:

**처리 흐름**:
- **POST 요청 (질문 제출)**:
  1. Clear 버튼이 눌린 경우: 세션의 메시지 히스토리를 초기화합니다.
  2. 질문이 제출된 경우:
     - 선택된 에이전트와 설정(autocorrect, history)을 세션에 저장합니다.
     - AgentRouter를 통해 에이전트를 호출합니다.
     - 응답을 세션의 메시지 히스토리에 추가합니다.
- **GET 요청 (페이지 렌더링)**:
  1. 활성 에이전트 목록을 템플릿에 전달합니다.
  2. 세션에 저장된 메시지 히스토리를 템플릿에 전달합니다.
  3. 사용자 설정을 템플릿에 전달합니다.

템플릿은 Jinja2를 사용하여 렌더링되며, JavaScript를 통해 실시간 상호작용을 제공합니다.

### 2.5 문서 처리

#### 2.5.1 문서 청킹 전략

문서는 RAG에 사용하기 전에 적절한 크기로 분할(청킹)되어야 합니다. 애플리케이션은 여러 청킹 전략을 지원합니다:

**지원하는 청킹 전략**:
- **RTCS (Recursive Text Character Splitter)**: 재귀적 문자 분할 방식입니다. Markdown 언어를 인식하여 구조를 고려한 분할을 수행합니다. 청크 크기와 오버랩은 설정에서 지정할 수 있습니다.
- **Sentence Tokenizer**: NLTK를 사용한 문장 단위 분할입니다. 문장의 경계를 인식하여 자연스러운 분할을 수행합니다.
- **PDF Loader**: PyPDF2를 사용하여 PDF 문서를 로드하고 페이지 단위로 분할합니다.

**문서 형식 지원**:
- PDF 파일 (`.pdf`)
- 텍스트 파일 (`.txt`, `.md`)
- 코드 파일 (`.py`, `.sh`)
- 설정 파일 (`.yaml`, `.json`, `.config`)

각 청크는 메타데이터를 포함하며, 원본 파일 경로, 청크 번호, 총 청크 수 등의 정보를 담고 있습니다.

---

## 3. 아키텍처

### 3.1 전체 시스템 아키텍처

시스템은 계층화된 아키텍처로 설계되어 있으며, 각 계층이 명확한 책임을 가집니다:

```
                    ┌─────────────┐
                    │   사용자    │
                    │  (브라우저) │
                    └──────┬──────┘
                           │
        ┌──────────────────┴──────────────────┐
        │                                        │
        ▼                                        ▼
┌──────────────┐                      ┌──────────────┐
│  Web UI      │                      │  인증 시스템 │
│  (Flask)     │◄─────────────────────┤  (OAuth/OIDC)│
│              │                      │  Flask-Login │
│  - Workspace │                      │  /login      │
│  - RAG       │                      │  /logout     │
│  - Settings  │                      │  /authorize  │
└──────┬───────┘                      └──────────────┘
        │
        │ HTTP Request / SocketIO
        ▼
┌─────────────────────────────────────────────────────┐
│         AIExpertService (Orchestrator)              │
│  ┌──────────────┐  ┌──────────────┐               │
│  │   설정 관리   │  │   로깅       │               │
│  │   (Cfg)      │  │  (Logger)    │               │
│  │  - app.yaml  │  │              │               │
│  │  - agents.yaml│ │              │               │
│  └──────────────┘  └──────────────┘               │
│                                                     │
│  ┌──────────────────────────────────────────────┐ │
│  │        Agent Router (중앙 라우터)             │ │
│  │  - 에이전트 초기화 및 관리                    │ │
│  │  - 요청 라우팅                                │ │
│  │  - 응답 래핑                                  │ │
│  └──────────────────────────────────────────────┘ │
│                                                     │
│  ┌──────────────────────────────────────────────┐ │
│  │  세션 관리 (Flask-Session)                   │ │
│  │  - messages (대화 히스토리)                   │ │
│  │  - connection (연결 모드)                     │ │
│  │  - settings (사용자 설정)                      │ │
│  │  - agent (선택된 에이전트)                    │ │
│  │  - user (사용자 정보)                         │ │
│  └──────────────────────────────────────────────┘ │
│                                                     │
│  ┌──────────────────────────────────────────────┐ │
│  │  SocketIO (실시간 통신)                       │ │
│  │  - connect, client_greet, server_greet        │ │
│  └──────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
        │
        ├──────────────┬──────────────┐
        ▼              ▼              ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐
   │ RAG     │  │ AICore  │  │ Future  │
   │ Agent   │  │ Agent   │  │ Agents  │
   │         │  │         │  │         │
   │ - wiki  │  │ - 범용  │  │ - Legal │
   │ - gitops│  │ - 오타  │  │ - Finance│
   └────┬────┘  └────┬────┘  └─────────┘
        │             │
        │             │
        ▼             ▼
┌──────────────┐  ┌──────────────┐
│ SAP HANA     │  │ SAP Gen AI   │
│ Vector DB    │  │ Hub          │
│              │  │              │
│ - 문서 임베딩│  │ - LLM 모델   │
│ - 벡터 검색 │  │ - 임베딩 모델 │
│ - MMR 검색  │  │              │
└──────────────┘  └──────────────┘

        [유틸리티 레이어]
        - Helper: 파일 처리, JSON 처리
        - Scheduler: 백그라운드 작업
```

### 3.2 계층별 상세 아키텍처

시스템은 다음과 같은 계층으로 구성됩니다:

#### Layer 1: 사용자 인터페이스 레이어
- 브라우저 기반 웹 인터페이스
- HTML/CSS/JavaScript
- SocketIO 클라이언트를 통한 실시간 통신

#### Layer 2: 웹 레이어
- **Flask 애플리케이션**: HTTP 요청 처리 및 라우팅
- **인증 시스템**: OAuth/OIDC 기반 인증, Flask-Login 통합
- **세션 관리**: Flask-Session을 통한 사용자 세션 관리
- **SocketIO**: 실시간 양방향 통신

#### Layer 3: 애플리케이션 레이어
- **AIExpertService**: 전체 애플리케이션의 오케스트레이션
- **설정 관리**: YAML 설정 파일 로드 및 환경 변수 관리
- **로깅 시스템**: 통합 로깅 인프라
- **Agent Router**: 에이전트 초기화, 라우팅, 응답 래핑

#### Layer 4: 에이전트 레이어
- **RAG Agent**: 문서 기반 질의응답 에이전트
- **AICore Agent**: 범용 AI 어시스턴트
- **Future Agents**: 확장 가능한 에이전트 구조

#### Layer 5: 처리 레이어
- **문서 처리**: Helper 모듈을 통한 파일 읽기, 청킹 등
- **벡터화**: 임베딩 모델을 통한 문서 벡터 변환
- **스케줄러**: 백그라운드 작업 스케줄링 (문서 임포트 등)

#### Layer 6: 외부 서비스 레이어
- **SAP HANA Cloud**: Vector Engine을 통한 벡터 데이터 저장 및 검색
- **SAP Generative AI Hub**: LLM 및 임베딩 모델 제공

### 3.3 데이터 흐름

사용자 질문이 답변으로 변환되는 상세한 과정은 다음과 같습니다:

#### 3.3.1 인증 및 세션 초기화

1. **사용자 접근**: 사용자가 웹 인터페이스에 접근합니다.
2. **인증 체크**: 로그인하지 않은 경우 `/login`으로 리다이렉트됩니다.
3. **OAuth 인증**: OIDC 제공자를 통해 사용자를 인증합니다.
4. **세션 생성**: 인증 성공 시 Flask-Session에 사용자 정보를 저장합니다.
   - `user`: 사용자 정보 (id, groups, mail 등)
   - `messages`: 빈 배열로 초기화
   - `connection`: 연결 모드 정보 (mode, modes)
   - `settings`: 빈 딕셔너리로 초기화
   - `agent`: 기본 에이전트 ID

#### 3.3.2 질문 처리 흐름

1. **사용자 질문 입력**: 사용자가 RAG 페이지에서 질문을 입력하고 에이전트를 선택합니다.
2. **요청 수신**: Flask 애플리케이션이 POST 요청을 받습니다.
3. **세션 업데이트**: 선택된 에이전트와 설정(autocorrect, history)을 세션에 저장합니다.
4. **에이전트 라우팅**: AgentRouter가 선택된 에이전트로 요청을 전달합니다.
5. **질문 전처리**: autocorrect 옵션이 활성화된 경우, AICore Agent를 사용하여 오타를 수정합니다.
6. **세션 메시지 추가**: 사용자 질문을 세션의 messages 배열에 추가합니다.
7. **에이전트 호출**: 선택된 에이전트의 `invoke()` 메서드를 호출합니다.

#### 3.3.3 RAG 처리 상세

RAG 에이전트가 선택된 경우:

1. **RAG 체인 선택**: 청킹 전략과 LLM 모델의 조합에 따라 적절한 RAG 체인을 선택합니다.
2. **질문 임베딩**: 질문을 임베딩 모델(SAP AI Hub)을 통해 벡터로 변환합니다.
3. **벡터 검색**: HANA Vector DB에서 유사한 문서를 검색합니다.
   - 검색 전략: MMR (Maximal Marginal Relevance) 또는 Similarity
   - 검색된 문서 수: 설정에 따라 k개 (예: 10개)
4. **문서 컨텍스트 구성**: 검색된 문서들을 컨텍스트로 구성합니다.
5. **LLM 호출**: 검색된 문서를 컨텍스트로 사용하여 LLM(SAP AI Hub)에 질문을 전달합니다.
6. **답변 생성**: LLM이 컨텍스트를 기반으로 답변을 생성합니다.

#### 3.3.4 응답 처리 및 반환

1. **응답 래핑**: AgentRouter가 에이전트의 응답을 일관된 형식으로 래핑합니다.
   - 성공 여부
   - 답변 내용
   - 출처 문서 (sources)
   - 메타데이터 (응답 시간, 타임스탬프, 연결 모드 등)
2. **세션 업데이트**: 생성된 응답을 세션의 messages 배열에 추가합니다.
   - `role`: "assistant"
   - `content`: 답변 내용
   - `sources`: 출처 문서 목록
   - `metadata`: 메타데이터
3. **템플릿 렌더링**: Jinja2 템플릿을 사용하여 HTML을 생성합니다.
   - 활성 에이전트 목록
   - 메시지 히스토리
   - 사용자 설정
4. **응답 반환**: 생성된 HTML을 사용자에게 반환합니다.
5. **화면 업데이트**: 브라우저에서 답변과 출처 문서가 표시됩니다.

### 3.4 컴포넌트 간 상호작용

시스템의 주요 컴포넌트들은 다음과 같이 상호작용합니다:

#### 웹 레이어 상호작용
- **Flask App ↔ 인증 모듈**: 인증이 필요한 페이지에 대해 권한을 체크합니다.
- **Flask App ↔ 세션 관리**: 모든 요청에서 세션을 초기화하고 관리합니다.
- **Flask App ↔ SocketIO**: 실시간 통신을 위해 SocketIO 이벤트를 처리합니다.

#### 애플리케이션 레이어 상호작용
- **AIExpertService ↔ 설정 관리**: 애플리케이션 시작 시 설정 파일을 로드합니다.
- **AIExpertService ↔ 로깅 시스템**: 모든 컴포넌트에서 통합 로깅을 사용합니다.
- **AIExpertService ↔ AgentRouter**: 에이전트를 초기화하고 요청을 라우팅합니다.

#### 에이전트 레이어 상호작용
- **AgentRouter ↔ RAGAgentLogic**: 라우터가 RAG 에이전트에 질문을 전달하고 응답을 받습니다.
- **AgentRouter ↔ AICore Agent**: 오타 수정 등 보조 기능을 위해 AICore Agent를 호출합니다.

#### 처리 레이어 상호작용
- **RAGAgentLogic ↔ Helper 모듈**: 문서 파일을 읽고 처리하는 데 Helper 함수를 사용합니다.
- **RAGAgentLogic ↔ HanaDB**: 벡터 스토어에서 문서를 검색합니다.
- **RAGAgentLogic ↔ SAP AI Hub**: 임베딩 모델과 LLM 모델을 호출합니다.
- **RAGAgentLogic ↔ Scheduler**: 문서 임포트 작업을 스케줄링합니다 (향후 사용).

#### 외부 서비스 상호작용
- **HanaDB ↔ SAP HANA Cloud**: 벡터 데이터를 저장하고 검색합니다.
- **RAGAgentLogic ↔ SAP Generative AI Hub**: LLM 및 임베딩 모델을 호출합니다.
- **AICore Agent ↔ SAP Generative AI Hub**: 직접 LLM 및 임베딩 모델을 호출합니다.

### 3.5 인증 및 권한 관리 흐름

인증 시스템은 다음과 같이 동작합니다:

1. **로그인 요청**: 사용자가 `/login`에 접근합니다.
2. **OIDC 리다이렉트**: 애플리케이션이 OIDC 제공자로 사용자를 리다이렉트합니다.
   - nonce를 생성하여 세션에 저장합니다.
3. **사용자 인증**: OIDC 제공자가 사용자를 인증합니다.
4. **콜백 처리**: 인증 성공 시 `/authorize` 엔드포인트로 리다이렉트됩니다.
5. **ID 토큰 파싱**: 애플리케이션이 ID 토큰을 파싱하여 사용자 정보를 추출합니다.
6. **User 객체 생성**: 사용자 정보로 User 객체를 생성합니다.
   - id (sub)
   - first_name, last_name
   - mail
   - groups (권한 그룹)
7. **Flask-Login 처리**: Flask-Login을 통해 사용자를 로그인 처리합니다.
8. **세션 저장**: 사용자 정보를 세션에 저장합니다.
9. **워크스페이스 리다이렉트**: `/workspace`로 리다이렉트합니다.

권한 체크는 `@permitted` 데코레이터를 통해 수행됩니다:
- 사용자의 groups와 요구되는 그룹을 비교합니다.
- 권한이 없으면 `forbidden.html` 템플릿을 렌더링합니다.

---

## 4. 주요 구성 요소

### 4.1 에이전트 시스템

#### 4.1.1 RAG 에이전트

RAG 에이전트는 `agents/rag/agent.py`에 구현되어 있으며, 문서 기반 질의응답의 핵심 로직을 담당합니다.

**주요 책임**:
- **문서 로드 및 청킹**: 다양한 형식의 문서를 로드하고 적절한 크기로 분할합니다.
- **벡터 스토어 관리**: 각 청킹 전략별로 벡터 스토어를 생성하고 관리합니다.
- **RAG 체인 구성 및 실행**: Retriever와 Document Chain을 결합하여 질문에 대한 답변을 생성합니다.

**핵심 메서드**:
- `main()`: 초기화 프로세스를 실행하여 모든 컴포넌트를 준비합니다.
- `invoke(query)`: 사용자 질문을 받아 RAG 체인을 실행하고 답변을 반환합니다.
- `docs_split()`: 문서를 청킹 전략에 따라 분할합니다.
- `set_rag_chain()`: RAG 체인을 구성합니다.

#### 4.1.2 AICore 에이전트

AICore 에이전트는 `agents/aicore/agent.py`에 구현되어 있으며, SAP AI Core에 직접 연결하여 범용 AI 기능을 제공합니다.

**주요 책임**:
- **SAP AI Core 직접 연결**: generative-ai-hub-sdk를 사용하여 SAP AI Core에 직접 연결합니다.
- **LLM 및 임베딩 모델 초기화**: Chat 모델과 Embedding 모델을 초기화합니다.
- **범용 AI 어시스턴트 기능 제공**: RAG 없이도 일반적인 AI 질의응답을 제공합니다.

**주요 용도**:
- 질문의 오타 수정
- 일반적인 AI 대화
- 다른 에이전트와의 통합 작업

### 4.2 웹 모듈

#### 4.2.1 Flask 서버

Flask 서버는 `modules/web/server.py`에서 초기화되며, 웹 인터페이스의 기반이 됩니다.

**구성 요소**:
- **Flask 앱 인스턴스**: 템플릿과 정적 파일을 관리합니다.
- **SocketIO 인스턴스**: 실시간 통신을 제공합니다. Eventlet 모드를 사용하여 비동기 처리를 지원합니다.
- **Session 관리**: Flask-Session을 사용하여 사용자 세션을 관리합니다. 현재는 파일 시스템 기반이지만, Redis 기반 세션도 지원할 수 있도록 설계되어 있습니다.

#### 4.2.2 인증

인증 모듈은 `modules/web/auth.py`에 구현되어 있으며, OAuth 기반 인증을 제공합니다.

**주요 기능**:
- **OAuth 인증**: SAP ID 서비스를 통한 OAuth 인증을 지원합니다.
- **`@login_required` 데코레이터**: 로그인이 필요한 페이지에 적용합니다.
- **`@permitted` 데코레이터**: 특정 권한 그룹에만 접근을 허용합니다.

### 4.3 설정 관리

#### 4.3.1 설정 클래스

설정 관리는 `modules/configs.py`의 `Cfg` 클래스를 통해 수행됩니다.

**특징**:
- **YAML 파일 로드**: YAML 형식의 설정 파일을 읽어옵니다.
- **객체식 접근**: 딕셔너리를 객체처럼 접근할 수 있습니다 (예: `cfg.webserver.loglevel`).
- **환경 변수 로드**: `.env` 파일에서 환경 변수를 로드합니다.
- **정적 메서드**: HANA 연결 정보, LLM 설정 등 공통 설정을 제공하는 정적 메서드를 포함합니다.

#### 4.3.2 설정 파일

애플리케이션은 두 가지 주요 설정 파일을 사용합니다:

- **`app.yaml`**: 애플리케이션 전반의 설정을 포함합니다. 웹서버 설정, 연결 모드, UI 설정 등이 포함됩니다.
- **`agents.yaml`**: 각 에이전트의 설정을 포함합니다. 에이전트별로 문서 경로, 청킹 전략, 검색 전략, 프롬프트 등을 정의합니다.

### 4.4 벡터 스토어

#### 4.4.1 HANA 벡터 스토어

HANA 벡터 스토어는 `modules/hana_vectorstores.py`에 구현되어 있으며, SAP HANA Cloud Vector Engine과의 통합을 제공합니다.

**주요 기능**:
- **SAP HANA Cloud Vector Engine 통합**: HANA의 벡터 데이터 타입을 활용합니다.
- **벡터 임베딩 저장 및 검색**: 문서의 임베딩 벡터를 저장하고 유사도 검색을 수행합니다.
- **MMR 검색 지원**: Maximal Marginal Relevance 알고리즘을 사용하여 다양성을 고려한 검색을 지원합니다.
- **Similarity 검색 지원**: 코사인 유사도 기반 검색을 지원합니다.

**테이블 구조**:
- **`VEC_TEXT`**: 문서 내용을 저장합니다 (NCLOB 타입).
- **`VEC_META`**: 문서의 메타데이터를 JSON 형식으로 저장합니다 (NCLOB 타입).
- **`VEC_VECTOR`**: 문서의 임베딩 벡터를 저장합니다 (REAL_VECTOR 또는 HALF_VECTOR 타입).

---

## 5. 에이전트 현황

### 5.1 활성 에이전트

#### 5.1.1 DCP Wiki MD (wikiv4)

**설정 위치**: `configs/agents.yaml`의 `wikiv4` 섹션

**특징**:
- **상태**: ✅ Active
- **문서 소스**: Confluence에서 내보낸 Markdown 문서를 사용합니다.
- **청킹 전략**: RTCS (Recursive Text Character Splitter)
  - Chunk size: 1000자
  - Overlap: 150자
- **검색 전략**: MMR (Maximal Marginal Relevance)
  - k: 10 (최종 반환할 문서 수)
  - fetch_k: 50 (MMR 알고리즘에 전달할 문서 수)
  - lambda_mult: 0.7 (다양성 조절, 0에 가까울수록 최대 다양성, 1에 가까울수록 최소 다양성)

**전문 분야**:
- Checkout order processes (주문 처리 프로세스)
- Configuration service manuals (설정 서비스 매뉴얼)
- API reference documentation (API 참조 문서)
- System implementation guides (시스템 구현 가이드)

#### 5.1.2 SAP AICore Service (aicore)

**설정 위치**: `configs/agents.yaml`의 `aicore` 섹션

**특징**:
- **상태**: ✅ Active
- **타입**: Integration Agent (통합 에이전트)
- **기능**: 범용 AI 어시스턴트로, RAG 없이도 일반적인 AI 질의응답을 제공합니다.
- **용도**: 
  - 질문의 오타 수정
  - 일반적인 AI 대화
  - 다른 에이전트와의 통합 작업

**초기화 과정**:
1. 환경 변수에서 LLM 및 Embedding 모델의 Deployment ID를 읽습니다.
2. ChatOpenAI와 OpenAIEmbeddings 인스턴스를 생성합니다.
3. 연결 테스트를 수행하여 초기화 상태를 확인합니다.

#### 5.1.3 GitOps Workload (gitops-workload)

**설정 위치**: `configs/agents.yaml`의 `gitops-workload` 섹션

**특징**:
- **상태**: ✅ Active
- **문서 형식**: txt, md, json, py, sh, yaml, config 등 다양한 형식을 지원합니다.
- **청킹**: RTCS 방식, 청크 크기 1000자, 오버랩 150자
- **검색**: MMR 방식 (k=10, fetch_k=50, lambda_mult=0.7)

이 에이전트는 GitOps 관련 문서를 전문으로 다루며, 코드 파일과 설정 파일도 처리할 수 있습니다.

### 5.2 비활성 에이전트

다음 에이전트들은 설정 파일에 정의되어 있으나 현재 비활성화되어 있습니다:

- **wiki**: PDF 기반 에이전트로, chunked multipage PDF를 사용합니다.
- **business**: 비즈니스 시나리오 전문 에이전트입니다. 현재 `active: false`로 설정되어 있습니다.
- **wikiv2, wikiv3**: 이전 버전의 Wiki 에이전트들로, Markdown 기반입니다.

### 5.3 향후 계획 에이전트

설정 파일에 향후 추가될 예정인 에이전트들이 정의되어 있습니다:

- **Legal Expert**: 법률 및 규정 준수 전문 에이전트입니다. 계약 조건, 개인정보 보호, 규정 준수 등의 전문 분야를 다룹니다.
- **Finance Expert**: 가격 정책 및 청구 전문 에이전트입니다. 가격 정책, 할인 계산, 청구 프로세스 등의 전문 분야를 다룹니다.

---

## 6. 기술 스택

### 6.1 백엔드 프레임워크

- **Python**: 3.12 이상 (권장: 3.13 이상)
- **Flask**: 3.1.2 이상 (웹 프레임워크)
- **Flask-SocketIO**: 5.5.1 이상 (실시간 통신)
- **Flask-Session**: 0.8.0 이상 (세션 관리)
- **Gunicorn**: 23.0.0 이상 (WSGI 서버, Eventlet worker 사용)

### 6.2 AI/ML 라이브러리

- **LangChain**: 0.3.27
  - `langchain-community`: 0.3.21
  - `langchain-core`: 0.3.79
  - `langchain-openai`: 0.3.35
- **SAP AI Hub SDK**: `generative-ai-hub-sdk[all]` 4.4.3
- **HANA ML**: `hana-ml` 2.26.25111700
- **LangChain HANA**: `langchain-hana` 0.2.1

### 6.3 데이터베이스

- **SAP HANA Cloud**: Vector Engine을 사용하여 벡터 데이터를 저장하고 검색합니다.
- **클라이언트**: `hdbcli` 2.26.25 (HANA 데이터베이스 클라이언트)

### 6.4 문서 처리

- **PDF**: `PyPDF2` 3.0.1 (PDF 문서 로드 및 파싱)
- **텍스트 처리**: `NLTK` 3.9.2 (문장 토크나이저)

### 6.5 인증 및 보안

- **OAuth**: `Authlib` 1.6.5 (OAuth 인증)
- **JWT**: `pyjwt`, `jwcrypto` (JWT 토큰 처리)

### 6.6 기타

- **환경 변수**: `python-dotenv` 1.0.0 (환경 변수 관리)
- **HTTP 클라이언트**: `requests` 2.25.0 이상
- **비동기**: `eventlet` 0.40.3 이상 (비동기 처리)

---

## 요약

### 핵심 아키텍처 특징

1. **설정 기반 확장성**: YAML 설정 파일만 수정하여 새 에이전트를 추가할 수 있습니다. UI 코드 변경이 필요 없습니다.
2. **모듈화된 구조**: 각 컴포넌트가 독립적으로 동작하며, 명확한 책임을 가집니다.
3. **RAG 파이프라인**: 문서 검색과 LLM 기반 답변 생성을 통합한 완전한 RAG 시스템입니다.
4. **실시간 통신**: SocketIO를 통한 실시간 응답 스트리밍을 지원합니다.
5. **세션 관리**: 사용자별 대화 히스토리를 관리하여 컨텍스트를 유지합니다.

### 주요 데이터 흐름

1. **사용자 질문 입력**: Flask 웹 인터페이스를 통해 질문이 입력됩니다.
2. **에이전트 라우팅**: AgentRouter가 사용자가 선택한 에이전트로 요청을 전달합니다.
3. **벡터 검색**: RAG 에이전트가 HANA Vector DB에서 관련 문서를 검색합니다.
4. **답변 생성**: 검색된 문서를 컨텍스트로 사용하여 LLM이 답변을 생성합니다.
5. **응답 표시**: 생성된 답변과 출처 문서가 웹 인터페이스에 표시됩니다.

### 확장 포인트

시스템은 다음과 같은 확장 포인트를 제공합니다:

1. **새 에이전트 추가**: `agents.yaml` 파일에 설정을 추가하고 에이전트 클래스를 구현하면 됩니다.
2. **새 문서 형식 지원**: `docs_split()` 메서드를 확장하여 새로운 문서 형식을 처리할 수 있습니다.
3. **새 검색 전략 추가**: RAG 체인 구성을 확장하여 새로운 검색 전략을 추가할 수 있습니다.
4. **새 연결 모드 추가**: `AgentRouter.invoke()` 메서드를 확장하여 새로운 연결 모드를 지원할 수 있습니다.

이러한 확장 포인트를 통해 시스템은 다양한 요구사항에 유연하게 대응할 수 있습니다.
