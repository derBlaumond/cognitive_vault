## 📖 상세 테스트 가이드

먼저 현재 상태를 확인하겠습니다.

[5 tools called]

좋습니다! 현재 상태를 파악했습니다. 이제 단계별로 자세히 설명드리겠습니다.

---

## 🎯 우선순위 1: Jest 테스트 실행 (필수)

### ✅ Step 1: Jest 에러 해결

**현재 문제:**
```
Error: Cannot find module '@jest/test-sequencer'
```

**해결 방법:**

```bash
# 1. Backend 폴더로 이동
cd /Users/injunpark/work/git/guava/apps/backend

# 2. node_modules 삭제 후 재설치
rm -rf node_modules
pnpm install

# 또는 루트에서 전체 재설치
cd /Users/injunpark/work/git/guava
pnpm install
```

---

### ✅ Step 2: 환경 변수 설정 확인

**필요한 환경 변수 (.env 파일):**

```bash
# /Users/injunpark/work/git/guava/apps/backend/.env

# Database
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/guava_db?schema=public"

# JWT
JWT_SECRET="your-secret-key-here"
JWT_EXPIRES_IN="7d"

# OpenAI (테스트용 - Mock 가능)
OPENAI_API_KEY="sk-..."

# CORS
FRONTEND_URL="http://localhost:5173"

# Email (선택사항 - 테스트에서는 불필요)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"
```

**환경 변수 확인:**
```bash
cd /Users/injunpark/work/git/guava/apps/backend
cat .env  # 환경 변수 확인
```

---

### ✅ Step 3: Unit Test 실행

**개별 테스트 실행:**

```bash
cd /Users/injunpark/work/git/guava/apps/backend

# Feature 1: 월간 설문 Unit Test
pnpm test surveys.service.spec.ts

# Feature 2: AI 콘텐츠 생성 Unit Test
pnpm test ai.service.spec.ts
```

**모든 Unit Test 실행:**
```bash
# src 폴더의 모든 .spec.ts 파일 실행
pnpm test

# Watch 모드 (코드 변경 시 자동 재실행)
pnpm test:watch
```

**커버리지 확인:**
```bash
# 커버리지 리포트 생성
pnpm test:cov

# 리포트 확인 (브라우저)
open coverage/lcov-report/index.html
```

---

### ✅ Step 4: E2E Test 실행

**E2E 테스트 실행 전 준비:**

1. **DB 마이그레이션 실행:**
```bash
cd /Users/injunpark/work/git/guava/apps/backend

# Prisma 마이그레이션 (DB 스키마 업데이트)
pnpm prisma:migrate

# 또는 push (개발 환경)
npx prisma db push

# 시드 데이터 생성 (선택사항)
pnpm prisma:seed
```

2. **E2E 테스트 실행:**

```bash
# Feature 1: 월간 설문 E2E Test
pnpm test surveys-monthly.e2e-spec.ts

# Feature 2: AI 콘텐츠 생성 E2E Test
pnpm test ai-content-generation.e2e-spec.ts

# 모든 E2E Test
pnpm test test/.*\\.e2e-spec\\.ts
```

**E2E 테스트 주의사항:**
- 실제 DB 연결 필요
- OpenAI API Key 필요 (또는 Mock 설정)
- 테스트 후 DB 정리 자동 실행

---

## 🎯 우선순위 2: 백엔드 서버 실행 & API 테스트 (권장)

### ✅ Step 5: 백엔드 서버 실행

**개발 모드로 실행:**

```bash
cd /Users/injunpark/work/git/guava/apps/backend

# Watch 모드 (코드 변경 시 자동 재시작)
pnpm dev

# 또는 일반 모드
pnpm start
```

**서버 실행 확인:**
```bash
# 새 터미널에서
curl http://localhost:3000/api/ai/status

# 응답 예시:
{
  "status": "ok",
  "provider": "openai",
  "model": "gpt-4"
}
```

**서버가 실행되면:**
- Backend: `http://localhost:3000`
- API Docs: `http://localhost:3000/api` (Swagger - 있다면)

---

### ✅ Step 6: Postman/Thunder Client로 API 테스트

#### 6-1. Thunder Client 설치 (VS Code 추천)

1. VS Code Extension 설치: `Thunder Client`
2. 사이드바에서 Thunder Client 아이콘 클릭

#### 6-2. 또는 Postman 사용

**Postman 다운로드:** https://www.postman.com/downloads/

---

### ✅ Step 7: API 테스트 시나리오

#### **Feature 1: 월간 설문 테스트**

**1. 월간 설문 생성**
```http
POST http://localhost:3000/api/surveys/monthly
Content-Type: application/json
Authorization: Bearer YOUR_JWT_TOKEN

{
  "hospitalId": "your-hospital-id",
  "title": "2026년 1월 월간 설문",
  "description": "테스트 설문",
  "month": 1,
  "year": 2026,
  "questions": [
    {
      "id": "q1",
      "type": "text",
      "question": "이번 달 집중하고 싶은 주제는?",
      "required": true
    },
    {
      "id": "q2",
      "type": "topic-selection",
      "question": "원하는 블로그 주제를 선택해주세요",
      "options": [
        {"value": "topic-1", "label": "다이어트"},
        {"value": "topic-2", "label": "스트레스 관리"}
      ],
      "required": true
    }
  ]
}
```

**2. 월간 주제 목록 조회**
```http
GET http://localhost:3000/api/surveys/monthly/topics
Authorization: Bearer YOUR_JWT_TOKEN
```

**3. 월간 설문 조회**
```http
GET http://localhost:3000/api/surveys/monthly/{hospitalId}
Authorization: Bearer YOUR_JWT_TOKEN
```

**4. 월간 설문 응답 제출**
```http
POST http://localhost:3000/api/surveys/{surveyId}/monthly-response
Content-Type: application/json
Authorization: Bearer YOUR_JWT_TOKEN

{
  "responses": {
    "q1": "다이어트와 건강 관리",
    "q2": ["topic-1", "topic-2"]
  }
}
```

---

#### **Feature 2: AI 콘텐츠 생성 테스트**

**1. 제목 생성**
```http
POST http://localhost:3000/api/ai/generate-titles
Content-Type: application/json
Authorization: Bearer YOUR_JWT_TOKEN

{
  "hospitalId": "your-hospital-id",
  "surveyId": "your-survey-id",
  "themeCount": 2,
  "selectedTopics": ["topic-1", "topic-2"],
  "generalsPerTheme": 5
}
```

**2. 제목 기반 본문 생성**
```http
POST http://localhost:3000/api/ai/generate-from-title
Content-Type: application/json
Authorization: Bearer YOUR_JWT_TOKEN

{
  "hospitalId": "your-hospital-id",
  "surveyId": "your-survey-id",
  "title": "다이어트 성공의 비결",
  "contentType": "THEME",
  "hashtag": "#다이어트"
}
```

**3. 제목 재생성**
```http
POST http://localhost:3000/api/ai/regenerate-titles
Content-Type: application/json
Authorization: Bearer YOUR_JWT_TOKEN

{
  "hospitalId": "your-hospital-id",
  "surveyId": "your-survey-id",
  "hashtag": "#다이어트",
  "count": 6
}
```

**4. 본문 재생성**
```http
POST http://localhost:3000/api/ai/regenerate-content/{contentId}
Content-Type: application/json
Authorization: Bearer YOUR_JWT_TOKEN

{
  "additionalInstructions": "더 전문적으로 작성해주세요"
}
```

---

### ✅ Step 8: JWT Token 발급 방법

**API 테스트를 위해 JWT Token이 필요합니다:**

```http
POST http://localhost:3000/api/auth/login
Content-Type: application/json

{
  "email": "admin@test.com",
  "password": "your-password"
}
```

**응답:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "user-id",
    "email": "admin@test.com",
    "role": "ADMIN"
  }
}
```

**Token 사용:**
```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

## 📋 체크리스트

### 우선순위 1 (필수)
```bash
□ 1. node_modules 재설치
   cd /Users/injunpark/work/git/guava/apps/backend
   rm -rf node_modules && pnpm install

□ 2. 환경 변수 확인
   cat .env

□ 3. DB 마이그레이션
   pnpm prisma:migrate

□ 4. Unit Test 실행
   pnpm test surveys.service.spec.ts
   pnpm test ai.service.spec.ts

□ 5. E2E Test 실행
   pnpm test surveys-monthly.e2e-spec.ts
   pnpm test ai-content-generation.e2e-spec.ts

□ 6. 커버리지 확인
   pnpm test:cov
   open coverage/lcov-report/index.html
```

### 우선순위 2 (권장)
```bash
□ 7. 백엔드 서버 실행
   pnpm dev

□ 8. 서버 상태 확인
   curl http://localhost:3000/api/ai/status

□ 9. JWT Token 발급
   POST /api/auth/login

□ 10. Feature 1 API 테스트 (Postman/Thunder Client)
   - 월간 설문 생성
   - 주제 목록 조회
   - 설문 응답 제출

□ 11. Feature 2 API 테스트
   - 제목 생성
   - 본문 생성
   - 재생성 API
```

---

## 🚨 문제 해결

### Jest 에러가 계속되면

**Option 1: Jest 재설치**
```bash
cd /Users/injunpark/work/git/guava/apps/backend
pnpm remove jest @types/jest ts-jest
pnpm add -D jest@^30.2.0 @types/jest@^30.0.0 ts-jest@^29.4.6
```

**Option 2: Workspace 전체 재설치**
```bash
cd /Users/injunpark/work/git/guava
rm -rf node_modules
rm -rf apps/backend/node_modules
pnpm install
```

### E2E 테스트가 실패하면

**원인 1: DB 연결 실패**
```bash
# PostgreSQL 실행 확인
psql -U postgres -c "SELECT 1"

# DB 존재 확인
psql -U postgres -l | grep guava
```

**원인 2: OpenAI API Key 없음**
```bash
# .env 파일 확인
cat apps/backend/.env | grep OPENAI_API_KEY
```

**원인 3: JWT Secret 없음**
```bash
# .env 파일 확인
cat apps/backend/.env | grep JWT_SECRET
```

---

## 💡 추가 정보 요청

제가 필요한 정보:
1. **.env 파일이 설정되어 있나요?**
2. **PostgreSQL이 실행 중인가요?**
3. **OpenAI API Key가 있나요?**
4. **백엔드 서버를 실행해보신 적이 있나요?**

이 정보를 알려주시면 더 정확한 도움을 드릴 수 있습니다! 🚀