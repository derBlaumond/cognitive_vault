# 시스템 아키텍처 검토 및 발표 가이드

## 1. 현재 아키텍처 다이어그램 검토

### 1.1 현재 다이어그램의 문제점

현재 `PRESENTATION_DETAILED.md`의 아키텍처 다이어그램은 다음과 같은 문제가 있습니다:

#### ❌ 누락된 주요 컴포넌트

1. **인증 시스템**
   - OAuth/OIDC 인증 모듈
   - Flask-Login 통합
   - `/login`, `/logout`, `/authorize` 라우트

2. **설정 관리 시스템**
   - `Cfg` 클래스 (YAML 설정 로더)
   - 환경 변수 관리
   - 설정 파일 (app.yaml, agents.yaml)

3. **로깅 시스템**
   - `Logger` 클래스
   - 통합 로깅 인프라

4. **세션 관리**
   - Flask-Session
   - 세션 데이터 구조 (messages, connection, settings, agent, user)

5. **유틸리티 모듈**
   - Helper 모듈 (파일 처리, JSON 처리 등)
   - Scheduler 모듈 (백그라운드 작업)

6. **A2A 프로토콜 구현**
   - A2A 서버 (Starlette 기반, 별도 서비스)
   - A2A 클라이언트
   - TaskManager

7. **SocketIO 통신**
   - 실시간 통신 레이어
   - 이벤트 핸들러

#### ⚠️ 부정확한 표현

1. **AICore Agent 연결**: AICore Agent도 SAP AI Hub를 직접 사용하지만 다이어그램에는 표시되지 않음
2. **에이전트 초기화**: 설정 파일 기반 동적 로딩이 명확하지 않음
3. **데이터 흐름**: 세션 관리와 인증이 데이터 흐름에 포함되지 않음

---

## 2. 개선된 시스템 아키텍처

### 2.1 전체 시스템 아키텍처 (개선안)

```
┌─────────────────────────────────────────────────────────────────┐
│                      사용자 (브라우저)                          │
└─────────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Web Layer (Flask + SocketIO)                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Workspace   │  │     RAG      │  │   Settings   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  인증 시스템 (OAuth/OIDC + Flask-Login)                  │  │
│  │  - /login, /logout, /authorize                           │  │
│  │  - 권한 체크 (@permitted)                                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  세션 관리 (Flask-Session)                               │  │
│  │  - messages, connection, settings, agent, user           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  SocketIO (실시간 통신)                                   │  │
│  │  - connect, client_greet, server_greet                   │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│              Application Layer (AIExpertService)                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  설정 관리 (Cfg)                                         │  │
│  │  - app.yaml, agents.yaml 로드                           │  │
│  │  - 환경 변수 관리 (.env)                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  로깅 시스템 (Logger)                                    │  │
│  │  - 통합 로깅 인프라                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Agent Router (중앙 라우터)                               │  │
│  │  - 에이전트 초기화 및 관리                               │  │
│  │  - 요청 라우팅                                           │  │
│  │  - 응답 래핑                                             │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ RAG Agent    │  │ AICore Agent │  │ Future Agents│
│ (wiki,       │  │ (Integration)│  │ (Legal,      │
│  business,   │  │              │  │  Finance)    │
│  gitops)     │  │              │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │
        │                 │
        ▼                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    RAG Processing Layer                           │
│  ┌──────────────────┐  ┌──────────────────┐                     │
│  │  문서 처리       │  │  벡터화          │                     │
│  │  - Helper        │  │  - Embedding     │                     │
│  │  - Scheduler     │  │    Model         │                     │
│  └──────────────────┘  └──────────────────┘                     │
└─────────────────────────────────────────────────────────────────┘
        │                 │
        ▼                 ▼
┌──────────────┐  ┌──────────────┐
│ SAP HANA     │  │ SAP Gen AI   │
│ Vector DB    │  │ Hub          │
│              │  │              │
│ - 문서 임베딩│  │ - LLM 모델   │
│ - 벡터 검색 │  │ - 임베딩 모델 │
│ - MMR 검색  │  │              │
└──────────────┘  └──────────────┘

┌─────────────────────────────────────────────────────────────────┐
│              A2A Protocol Layer (별도 서비스)                     │
│  ┌──────────────┐  ┌──────────────┐                            │
│  │ A2A Server   │  │ A2A Client   │                            │
│  │ (Starlette)  │  │ (httpx)      │                            │
│  └──────────────┘  └──────────────┘                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  TaskManager (비동기 작업 관리)                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 계층별 상세 아키텍처

#### Layer 1: 사용자 인터페이스
- 브라우저 기반 웹 인터페이스
- HTML/CSS/JavaScript
- SocketIO 클라이언트

#### Layer 2: 웹 레이어
- **Flask 애플리케이션**: HTTP 요청 처리
- **인증 시스템**: OAuth/OIDC, Flask-Login
- **세션 관리**: Flask-Session (파일 시스템 기반)
- **SocketIO**: 실시간 통신

#### Layer 3: 애플리케이션 레이어
- **AIExpertService**: 메인 애플리케이션 오케스트레이션
- **설정 관리**: YAML 설정 로드 및 환경 변수 관리
- **로깅 시스템**: 통합 로깅
- **Agent Router**: 에이전트 라우팅 및 관리

#### Layer 4: 에이전트 레이어
- **RAG Agent**: 문서 기반 질의응답
- **AICore Agent**: 범용 AI 어시스턴트
- **Future Agents**: 확장 가능한 구조

#### Layer 5: 처리 레이어
- **문서 처리**: Helper 모듈 (파일 읽기, 청킹 등)
- **벡터화**: 임베딩 모델
- **스케줄러**: 백그라운드 작업 (문서 임포트 등)

#### Layer 6: 외부 서비스
- **SAP HANA Cloud**: Vector Engine
- **SAP Generative AI Hub**: LLM 및 임베딩 모델

#### Layer 7: A2A 프로토콜 (별도)
- **A2A Server**: Starlette 기반 비동기 서버
- **A2A Client**: HTTP 클라이언트
- **TaskManager**: 비동기 작업 관리

---

## 3. 데이터 흐름 상세 분석

### 3.1 사용자 질문 처리 흐름 (상세)

```
1. 사용자 브라우저
   │
   ├─→ [인증 체크] → 로그인 필요 시 /login으로 리다이렉트
   │
   ├─→ [POST /rag] → 질문 제출
   │
   ▼
2. Flask 애플리케이션 (AIExpertService)
   │
   ├─→ [세션 초기화] → session() 메서드 호출
   │   ├─ messages: []
   │   ├─ connection: {mode, modes}
   │   ├─ settings: {}
   │   └─ agent: 기본값
   │
   ├─→ [권한 체크] → @login_required 데코레이터
   │
   ├─→ [요청 파싱] → question, agent, autocorrect, history
   │
   ├─→ [세션 업데이트] → agent, settings 저장
   │
   ▼
3. Agent Router
   │
   ├─→ [질문 전처리] → autocorrect 옵션 시 AICore Agent 호출
   │
   ├─→ [세션 메시지 추가] → 사용자 질문을 messages에 추가
   │
   ├─→ [에이전트 선택] → agents[agent]["agent"] 선택
   │
   ├─→ [에이전트 호출] → agent.invoke(question)
   │
   ▼
4. RAG Agent (예시)
   │
   ├─→ [RAG 체인 선택] → 청킹 전략과 LLM 조합 선택
   │
   ├─→ [질문 임베딩] → Embedding Model 호출 (SAP AI Hub)
   │
   ├─→ [벡터 검색] → HANA Vector DB에서 유사 문서 검색
   │   ├─ MMR 또는 Similarity 검색
   │   └─ k개 문서 반환
   │
   ├─→ [LLM 호출] → 검색된 문서를 컨텍스트로 전달
   │   └─ SAP AI Hub의 LLM 모델 호출
   │
   ├─→ [답변 생성] → LLM이 컨텍스트 기반 답변 생성
   │
   ▼
5. 응답 래핑 (Agent Router)
   │
   ├─→ [메타데이터 추가] → 응답 시간, 타임스탬프, 연결 모드
   │
   ├─→ [형식 통일] → 일관된 딕셔너리 형식으로 변환
   │
   ▼
6. Flask 애플리케이션
   │
   ├─→ [세션 업데이트] → 응답을 messages에 추가
   │   └─ {role: "assistant", content, sources, metadata}
   │
   ├─→ [템플릿 렌더링] → Jinja2 템플릿에 데이터 전달
   │
   ▼
7. 사용자 브라우저
   │
   └─→ [응답 표시] → 답변과 출처 문서 표시
```

### 3.2 인증 흐름

```
1. 사용자 → /login 접근
   │
   ▼
2. Flask → OIDC 제공자로 리다이렉트
   │
   ├─→ nonce 생성 및 세션에 저장
   │
   ▼
3. OIDC 제공자 → 사용자 인증
   │
   ▼
4. OIDC 제공자 → /authorize 콜백
   │
   ▼
5. Flask → ID 토큰 파싱
   │
   ├─→ User 객체 생성
   │   ├─ id (sub)
   │   ├─ first_name, last_name
   │   ├─ mail
   │   └─ groups
   │
   ├─→ Flask-Login으로 로그인 처리
   │
   ├─→ 세션에 사용자 정보 저장
   │
   ▼
6. Flask → /workspace로 리다이렉트
```

---

## 4. 발표 방법론

### 4.1 발표 구조 제안

#### Phase 1: 전체 개요 (2-3분)

**목표**: 청중에게 시스템의 전체적인 그림을 제시

**발표 포인트:**
1. **문제 정의**: DCP 문서가 많고 복잡하여 검색이 어려움
2. **해결책**: RAG 기반 AI 서비스로 문서를 검색하고 답변 생성
3. **핵심 가치**: 설정 기반 확장 가능한 아키텍처

**시각 자료:**
- 간단한 전체 아키텍처 다이어그램 (현재 다이어그램 사용 가능)
- 사용자 시나리오 (질문 → 답변)

#### Phase 2: 계층별 상세 설명 (10-12분)

**목표**: 각 계층의 역할과 상호작용을 설명

**발표 순서:**

**2-1. 사용자 인터페이스 레이어 (1분)**
- 웹 기반 인터페이스
- 주요 페이지 소개
- 실시간 통신 (SocketIO)

**2-2. 웹 레이어 (2-3분)**
- Flask 애플리케이션 구조
- 인증 시스템 (OAuth/OIDC)
  - "보안이 중요한 엔터프라이즈 환경이므로 OAuth 기반 인증을 사용합니다"
  - 권한 기반 접근 제어
- 세션 관리
  - "사용자별 대화 히스토리를 관리하여 컨텍스트를 유지합니다"

**2-3. 애플리케이션 레이어 (2-3분)**
- AIExpertService의 역할
- 설정 관리 시스템
  - "YAML 기반 설정으로 코드 변경 없이 확장 가능합니다"
- Agent Router
  - "중앙에서 모든 에이전트를 관리하고 라우팅합니다"
  - 동적 에이전트 로딩 과정 설명

**2-4. 에이전트 레이어 (2-3분)**
- RAG Agent 구조
  - "문서 검색과 답변 생성을 통합한 RAG 파이프라인입니다"
- AICore Agent
  - "범용 AI 기능과 오타 수정 등을 제공합니다"
- 확장 가능성
  - "설정 파일만 수정하여 새 에이전트를 추가할 수 있습니다"

**2-5. 처리 레이어 (1-2분)**
- 문서 처리 (Helper 모듈)
- 벡터화 과정
- 스케줄러 (향후 사용)

**2-6. 외부 서비스 (1분)**
- SAP HANA Cloud Vector Engine
- SAP Generative AI Hub

**시각 자료:**
- 계층별 아키텍처 다이어그램
- 각 계층의 주요 컴포넌트 리스트
- 컴포넌트 간 상호작용 다이어그램

#### Phase 3: 데이터 흐름 시연 (3-5분)

**목표**: 실제 질문이 답변으로 변환되는 과정을 단계별로 설명

**발표 방법:**

**3-1. 시나리오 제시**
- "사용자가 'DCP checkout 프로세스는 어떻게 동작하나요?'라고 질문했습니다"

**3-2. 단계별 설명**
1. **사용자 입력** → 웹 인터페이스
2. **인증 체크** → 로그인 확인
3. **세션 관리** → 메시지 히스토리 저장
4. **에이전트 라우팅** → wikiv4 에이전트 선택
5. **질문 임베딩** → 벡터로 변환
6. **벡터 검색** → HANA DB에서 유사 문서 검색
7. **LLM 호출** → 검색된 문서를 컨텍스트로 전달
8. **답변 생성** → LLM이 답변 생성
9. **응답 반환** → 출처와 함께 표시

**시각 자료:**
- 데이터 흐름 다이어그램
- 각 단계별 데이터 예시
- 실제 화면 캡처 (가능한 경우)

#### Phase 4: 핵심 특징 강조 (2-3분)

**목표**: 시스템의 핵심 가치와 차별점 강조

**발표 포인트:**

1. **설정 기반 확장성**
   - "UI 코드 변경 없이 새 에이전트 추가 가능"
   - "YAML 설정만으로 모든 것을 관리"

2. **모듈화된 구조**
   - "각 컴포넌트가 독립적으로 동작"
   - "명확한 책임 분리"

3. **엔터프라이즈급 기능**
   - "OAuth 기반 인증"
   - "권한 기반 접근 제어"
   - "세션 관리 및 대화 히스토리"

4. **SAP 생태계 통합**
   - "SAP HANA Cloud Vector Engine"
   - "SAP Generative AI Hub"

**시각 자료:**
- 핵심 특징 요약 슬라이드
- 아키텍처 다이어그램에서 확장 포인트 강조

#### Phase 5: Q&A 준비 (1-2분)

**예상 질문과 답변:**

**Q: 성능은 어떤가요?**
A: Direct Mode를 사용하면 빠른 응답을 제공합니다. 벡터 검색은 HANA의 인덱스를 활용하여 효율적입니다.

**Q: 확장성은?**
A: 설정 파일 기반으로 새 에이전트를 쉽게 추가할 수 있습니다. 각 에이전트는 독립적으로 동작합니다.

**Q: 보안은?**
A: OAuth/OIDC 기반 인증을 사용하며, 그룹 기반 권한 체크를 지원합니다.

**Q: A2A 프로토콜은 어떻게 사용되나요?**
A: 현재는 별도 서비스로 구현되어 있으며, 향후 Flask 앱과 통합할 예정입니다.

---

## 5. 발표 시 주의사항

### 5.1 청중에 따른 조정

**기술진 대상:**
- 아키텍처 상세 설명 강화
- 컴포넌트 간 상호작용 상세 설명
- 확장 포인트와 설계 결정 설명

**경영진/비즈니스 대상:**
- 비즈니스 가치 강조
- 사용자 시나리오 중심
- ROI 및 확장 가능성 강조

**혼합 청중:**
- 전체 개요 → 핵심 특징 → 기술 상세 순서
- 각 섹션에서 비즈니스 가치와 기술적 우수성 균형

### 5.2 시각 자료 활용

**권장 슬라이드 구성:**

1. **제목 슬라이드**: 프로젝트명과 목적
2. **문제 정의**: 왜 이 시스템이 필요한가
3. **전체 아키텍처**: 한 눈에 보는 시스템 구조
4. **계층별 상세**: 각 레이어의 역할
5. **데이터 흐름**: 질문 → 답변 과정
6. **핵심 특징**: 차별화 포인트
7. **에이전트 현황**: 현재 구현 상태
8. **확장 계획**: 향후 로드맵
9. **Q&A**

**다이어그램 활용 팁:**
- 한 번에 모든 것을 보여주지 말고, 단계적으로 공개
- 애니메이션 효과로 데이터 흐름 강조
- 색상으로 계층 구분 (예: 웹=파란색, 에이전트=초록색, 외부=회색)

### 5.3 발표 스타일

**권장 접근법:**

1. **스토리텔링**: 사용자 시나리오로 시작
   - "개발자가 DCP 문서를 검색하는데 시간이 오래 걸렸습니다..."
   - "이 시스템을 통해 몇 초 만에 정확한 답변을 받을 수 있습니다"

2. **점진적 공개**: 단순한 그림에서 복잡한 그림으로
   - 먼저 전체 흐름
   - 그 다음 각 계층 상세
   - 마지막으로 컴포넌트 간 상호작용

3. **비유 활용**: 기술적 개념을 쉽게 설명
   - "Agent Router는 교통 경찰과 같습니다"
   - "RAG는 도서관에서 책을 찾아 요약해주는 것과 같습니다"

4. **실제 예시**: 가능하면 데모나 스크린샷
   - 실제 질문과 답변 예시
   - UI 화면 공유

---

## 6. 개선된 아키텍처 다이어그램 (최종)

### 6.1 전체 시스템 아키텍처 (권장)

```
                    ┌─────────────┐
                    │   사용자    │
                    └──────┬──────┘
                           │
        ┌──────────────────┴──────────────────┐
        │                                        │
        ▼                                        ▼
┌──────────────┐                      ┌──────────────┐
│  Web UI      │                      │  인증        │
│  (Flask)     │◄─────────────────────┤  (OAuth)     │
└──────┬───────┘                      └──────────────┘
        │
        │ HTTP Request
        ▼
┌─────────────────────────────────────────────────────┐
│         AIExpertService (Orchestrator)              │
│  ┌──────────────┐  ┌──────────────┐               │
│  │   설정 관리   │  │   로깅       │               │
│  │   (Cfg)      │  │  (Logger)    │               │
│  └──────────────┘  └──────────────┘               │
│                                                     │
│  ┌──────────────────────────────────────────────┐ │
│  │        Agent Router (중앙 라우터)             │ │
│  │  - 에이전트 초기화 및 관리                    │ │
│  │  - 요청 라우팅                                │ │
│  │  - 응답 래핑                                  │ │
│  └──────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
        │
        ├──────────────┬──────────────┐
        ▼              ▼              ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐
   │ RAG     │  │ AICore  │  │ Future  │
   │ Agent   │  │ Agent   │  │ Agents  │
   └────┬────┘  └────┬────┘  └─────────┘
        │             │
        │             │
        ▼             ▼
┌──────────────┐  ┌──────────────┐
│ SAP HANA     │  │ SAP Gen AI   │
│ Vector DB    │  │ Hub          │
└──────────────┘  └──────────────┘

        [세션 관리] ──► Flask-Session
        [실시간 통신] ──► SocketIO
        [유틸리티] ──► Helper, Scheduler
```

### 6.2 데이터 흐름 다이어그램 (권장)

```
사용자 질문
    │
    ▼
[인증 체크] ──► 로그인 필요 시 /login
    │
    ▼
[세션 초기화] ──► messages, connection, settings
    │
    ▼
[Agent Router] ──► 에이전트 선택
    │
    ├─→ [AICore] ──► 오타 수정 (옵션)
    │
    ▼
[RAG Agent]
    │
    ├─→ [임베딩] ──► SAP AI Hub
    │
    ├─→ [벡터 검색] ──► SAP HANA Vector DB
    │
    ├─→ [문서 검색] ──► MMR/Similarity
    │
    └─→ [LLM 호출] ──► SAP AI Hub
    │
    ▼
[답변 생성]
    │
    ▼
[응답 래핑] ──► 메타데이터 추가
    │
    ▼
[세션 업데이트] ──► messages에 추가
    │
    ▼
[템플릿 렌더링] ──► Jinja2
    │
    ▼
사용자에게 표시
```

---

## 7. 발표 체크리스트

### 발표 전 준비

- [ ] 아키텍처 다이어그램 최신화 (인증, 세션, 유틸리티 추가)
- [ ] 데이터 흐름 다이어그램 준비
- [ ] 실제 질문/답변 예시 준비
- [ ] UI 스크린샷 준비 (가능한 경우)
- [ ] 예상 질문과 답변 준비
- [ ] 발표 시간에 맞춰 슬라이드 조정

### 발표 중

- [ ] 전체 개요부터 시작
- [ ] 계층별로 점진적 설명
- [ ] 실제 예시 활용
- [ ] 청중 반응 확인
- [ ] 시간 관리

### 발표 후

- [ ] Q&A 대응
- [ ] 피드백 수집
- [ ] 후속 자료 제공 (필요시)

---

## 8. 결론 및 권장사항

### 현재 아키텍처 다이어그램 평가

**정확도: 70/100**
- 핵심 구조는 정확하나 중요한 컴포넌트 누락
- 인증, 세션, 설정 관리 등이 빠져있음

**완전성: 60/100**
- 기본적인 흐름은 표현되나 세부사항 부족
- A2A 프로토콜, 유틸리티 모듈 등 누락

### 개선 권장사항

1. **즉시 개선**: 인증 시스템과 세션 관리를 다이어그램에 추가
2. **다음 단계**: 계층별 상세 다이어그램 추가
3. **선택적**: A2A 프로토콜 레이어 추가 (별도 서비스임을 명시)

### 발표 전략

1. **전체 → 부분**: 먼저 전체 그림을 보여주고, 각 부분을 상세히 설명
2. **사용자 중심**: 기술보다는 사용자 시나리오로 시작
3. **점진적 공개**: 한 번에 모든 것을 보여주지 말고 단계적으로
4. **비유 활용**: 복잡한 개념을 쉽게 설명
5. **실제 예시**: 가능하면 데모나 스크린샷 활용

이 가이드를 따라 발표하면 청중이 시스템을 더 잘 이해할 수 있을 것입니다.

